<UserControl x:Class="MeetingPlanner.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:MeetingPlanner.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <Style TargetType="Button" x:Key="PrimaryButtonStyle">
            <Setter Property="Background" Value="#6a7eee"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="Margin" Value="0,10,0,0"/>

        </Style>

        <Style TargetType="Button" x:Key="DangerButtonStyle" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#FF4444"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#80c0ff"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#6495ED">
        <Grid.RowDefinitions>
            <RowDefinition Height="98*"/>
            <RowDefinition Height="127*"/>
        </Grid.RowDefinitions>
        <TabControl BorderThickness="0" Margin="0,0,0,-0.4" Grid.RowSpan="2">
            <!-- Вкладка аккаунта -->
            <TabItem Header="Аккаунт">
                <Grid Margin="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Верхняя часть с аватаром -->
                    <Border Grid.Row="0" Background="#F0F5FF" CornerRadius="10" Padding="20">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Image x:Name="AvatarImage" 
                                   Width="120" 
                                   Height="120" 
                                   Margin="0,0,30,0" 
                                   Source="{Binding AvatarPath}"/>

                            <StackPanel VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                    <Label Content="Имя:" 
                                           Foreground="#6a7eee" 
                                           FontWeight="Bold"
                                           MinWidth="80"/>
                                    <Label Content="{Binding FirstName}" 
                                           FontSize="14"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal">
                                    <Label Content="Фамилия:" 
                                           Foreground="#6a7eee" 
                                           FontWeight="Bold"
                                           MinWidth="80"/>
                                    <Label Content="{Binding LastName}" 
                                           FontSize="14"/>
                                </StackPanel>

                                <Button Content="Сменить аватар" 
                                        Click="ChangeAvatarButton_Click" 
                                        Style="{StaticResource PrimaryButtonStyle}"
                                        Margin="30,15,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Нижняя часть с редактированием -->
                    <Border Grid.Row="1" Background="#F0F5FF" CornerRadius="10" Margin="0,20,0,0" Padding="20">
                        <StackPanel>
                            <Label Content="Редактирование профиля" 
                                   Foreground="#6a7eee" 
                                   FontWeight="Bold"
                                   FontSize="16"
                                   Margin="0,0,0,15"/>

                            <Label Content="Имя" Foreground="#555"/>
                            <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"/>

                            <Label Content="Фамилия" Foreground="#555"/>
                            <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"/>

                            <Button Content="Сохранить изменения" 
                                    Command="{Binding SaveChangesCommand}" 
                                    Style="{StaticResource PrimaryButtonStyle}"/>

                            <Button Content="Выйти" 
                                    Command="{Binding LogoutCommand}" 
                                    Style="{StaticResource DangerButtonStyle}"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>

            <!-- Вкладка календаря -->
            <TabItem Header="Календарь">
                <Border Background="#F0F5FF" CornerRadius="10" Margin="10">
                    <Calendar HorizontalAlignment="Stretch" 
                              VerticalAlignment="Stretch" 
                              Margin="10"
                              BorderThickness="0"/>
                </Border>
            </TabItem>

            <!-- Вкладка контактов -->
            <TabItem Header="Контакты">
                <views:ContactsView DataContext="{Binding ContactsViewModel}" Margin="0,-5,-5,-2.8"/>
            </TabItem>
        </TabControl>

        <!-- Декоративные элементы -->
        <Ellipse Width="150" Height="150" 
                 Fill="#80c0ff" 
                 HorizontalAlignment="Right" 
                 VerticalAlignment="Bottom"
                 Margin="0,0,20,19.6"
                 Opacity="0.2" Grid.Row="1"/>
    </Grid>
</UserControl>
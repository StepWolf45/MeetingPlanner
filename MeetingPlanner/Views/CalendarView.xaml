<UserControl x:Class="MeetingPlanner.Views.CalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:converters="clr-namespace:MeetingPlanner.Converters"
             mc:Ignorable="d"
             d:DesignHeight="550" d:DesignWidth="800">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:HasEventConverter x:Key="HasEventConverter"/>
        <converters:TimeSpanConverter x:Key="TimeSpanConverter"/>
        <Style TargetType="Button" x:Key="PrimaryButton">
            <Setter Property="Background" Value="#6a7eee"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10 5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0 0 0 10"/>
            <Setter Property="Padding" Value="5"/>
        </Style>


    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <!-- Calendar -->
        <Calendar Grid.Column="0" Margin="10"
                  SelectedDate="{Binding SelectedDate, Mode=TwoWay}"
                  DisplayDate="{Binding SelectedDate}"
                  SelectionMode="SingleDate"
                  SelectedDatesChanged="Calendar_SelectedDatesChanged">
            <Calendar.CalendarDayButtonStyle>
                <Style TargetType="CalendarDayButton">
                    <Setter Property="Background" Value="Transparent"/>
                    <Style.Triggers>
                        <!-- Selected date -->
                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                            <Setter Property="Background" Value="LightBlue"/>
                        </DataTrigger>

                        <!-- Dates with events -->
                        <DataTrigger Value="True">
                            <DataTrigger.Binding>
                                <MultiBinding Converter="{StaticResource HasEventConverter}">
                                    <Binding Path="DataContext.Events" 
                                             RelativeSource="{RelativeSource AncestorType=Calendar}"/>
                                    <Binding Path="Date"/>
                                </MultiBinding>
                            </DataTrigger.Binding>
                            <Setter Property="Background" Value="LightGreen"/>
                            <Setter Property="ToolTip">
                                <Setter.Value>
                                    <ToolTip>
                                        <ItemsControl ItemsSource="{Binding DataContext.GetEventsForDate(Date), 
                                                             RelativeSource={RelativeSource AncestorType=Calendar}}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <StackPanel Margin="5">
                                                        <TextBlock Text="{Binding Title}" FontWeight="Bold"/>
                                                        <StackPanel Orientation="Horizontal">
                                                            <TextBlock Text="{Binding StartTime, StringFormat='{}{0:HH:mm}'}"/>
                                                            <TextBlock Text=" - "/>
                                                            <TextBlock Text="{Binding EndTime, StringFormat='{}{0:HH:mm}'}"/>
                                                        </StackPanel>
                                                    </StackPanel>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ToolTip>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Calendar.CalendarDayButtonStyle>
        </Calendar>

        <!-- Event Form -->
        <Border Grid.Column="1" Background="#3F888F" Padding="15"
               Visibility="{Binding IsEventFormVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="New Event" FontSize="16" FontWeight="Bold" Margin="0 0 0 10"/>

                    <Label Content="Title*"/>
                    <TextBox Text="{Binding NewEvent.Title, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Content="Description"/>
                    <TextBox Text="{Binding NewEvent.Description}" AcceptsReturn="True" MinHeight="60"/>

                    <Label Content="Location"/>
                    <TextBox Text="{Binding NewEvent.Location}"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0 0 5 0">
                            <Label Content="Start Date"/>
                            <DatePicker SelectedDate="{Binding StartDate, Mode=TwoWay}"/>
                            <Label Content="Start Time"/>
                            <ComboBox SelectedItem="{Binding StartTime, Mode=TwoWay}"
                                      ItemsSource="{Binding TimeSlots}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Converter={StaticResource TimeSpanConverter}}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="5 0 0 0">
                            <Label Content="End Date"/>
                            <DatePicker SelectedDate="{Binding EndDate, Mode=TwoWay}"/>
                            <Label Content="End Time"/>
                            <ComboBox SelectedItem="{Binding EndTime, Mode=TwoWay}"
                                      ItemsSource="{Binding TimeSlots}">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Converter={StaticResource TimeSpanConverter}}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <Label Content="Invite Friends"/>
                    <ComboBox ItemsSource="{Binding Friends}"
                              SelectedItem="{Binding SelectedFriend}"
                              DisplayMemberPath="FullName"/>

                    <Button Content="Add" Command="{Binding AddAttendeeCommand}"
                           CommandParameter="{Binding SelectedFriend}"
                           Style="{StaticResource PrimaryButton}"/>

                    <ListView ItemsSource="{Binding SelectedAttendees}" Height="100" Margin="0 10">
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding FullName}" VerticalAlignment="Center"/>
                                    <Button Content="Remove" Margin="10 0 0 0"
                                           Command="{Binding DataContext.RemoveAttendeeCommand,
                                                   RelativeSource={RelativeSource AncestorType=UserControl}}"
                                           CommandParameter="{Binding}"
                                           Style="{StaticResource PrimaryButton}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <Button Content="Create Event" Command="{Binding CreateEventCommand}"
                           Style="{StaticResource PrimaryButton}" Margin="0 10"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Event Details Popup -->
        <Border Grid.ColumnSpan="2" Background="#80000000" Padding="20"
               Visibility="{Binding IsEventDetailsVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Background="White" Padding="20" CornerRadius="5">
                <StackPanel MinWidth="300">
                    <TextBlock Text="Event Details" FontSize="18" FontWeight="Bold" Margin="0 0 0 10"/>

                    <TextBlock Text="{Binding SelectedEvent.Title}" FontSize="16" FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding SelectedEvent.Description}" Margin="0 5"/>
                    <TextBlock Text="{Binding SelectedEvent.Location}" Margin="0 5"/>

                    <TextBlock Text="{Binding SelectedEvent.StartTime, StringFormat='Start: {0:g}'}"/>
                    <TextBlock Text="{Binding SelectedEvent.EndTime, StringFormat='End: {0:g}'}"/>

                    <TextBlock Text="Attendees:" FontWeight="Bold" Margin="0 10 0 5"/>
                    <ItemsControl ItemsSource="{Binding SelectedEvent.Attendees}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding FullName}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <Button Content="Close" Command="{Binding CloseEventDetailsCommand}"
                           Style="{StaticResource PrimaryButton}" Margin="0 20 0 0" HorizontalAlignment="Right"/>
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</UserControl>